# 10 · 카테고리

설계: `../CATEGORIES.md`. 1~3단계 2026-09-09 구현. 2026-09-09 01:30 실기기 시도: CAT-01(v4→v5 업데이트, 크래시 없음, `user_version=5`) 통과 후 기기가 잠겨(PIN) 나머지는 보류 — 잠금 해제 후 이어서.

| ID | 시나리오 | 기대 결과 | 상태 | 메모 |
|---|---|---|---|---|
| CAT-01 | v4(이전 빌드) 설치 위에 업데이트 | 크래시 없음, 기존 큐·원장 유지, `category`/`media_category` 표 생성 | ✅ 2026-09-09 | `user_version=5`, 6,119개 로드, 고아 정리의 휴지통 조회(0건) 확인 |
| CAT-02 | 13개 선택 → 하단 바 카테고리 → 새 카테고리 "여행" 만들고 적용 | 스낵바 "13개 항목의 카테고리를 변경했습니다", 선택 유지 | ⬜ | 썸네일 오른쪽 위 색 점 |
| CAT-03 | 일부만 붙은 상태에서 피커 열기 | 부분(◪) 표시, 탭하면 체크 → 빈 칸 → 체크 순환. 그대로 두면 변경 없음 | ⬜ | `CategoryPickerState` 단위 테스트 통과 |
| CAT-04 | ⋮ → 카테고리 → "여행" 적용 | 목록이 그 항목만, 상단 제목 "여행", 카테고리 바 표시, 상세보기 스와이프 범위 일치 | ⬜ | `MediaViewerKey.categoryIds` |
| CAT-05 | 카테고리 둘 선택 | OR — 둘 중 하나라도 붙은 항목, 제목 "카테고리 2개" | ⬜ | |
| CAT-06 | 카테고리 + 기간 + 즐겨찾기 동시 | AND 조합, 기간 달력엔 카테고리 사진 있는 날만 점 | ⬜ | `dayCounts` 는 카테고리 필터 뒤 목록 |
| CAT-07 | "미분류" | 카테고리 없는 항목만, 제목 "미분류" | ⬜ | |
| CAT-08 | 카테고리 붙은 항목을 휴지통 → 복원 | 카테고리 유지 | ⬜ | 고아 정리는 휴지통 ID 를 살아 있는 것으로 봄 |
| CAT-09 | 관리 화면에서 이름·색 변경 | 필터 바·칩에 즉시 반영 | ⬜ | 설정 → 카테고리 관리 / 필터 시트 "관리" |
| CAT-10 | 같은 이름(대소문자만 다름)으로 생성 | "같은 이름의 카테고리가 있습니다" | ⬜ | |
| CAT-11 | 카테고리 삭제(확인 문구에 개수) | 항목에서 제거, 사진은 남음. 켜져 있던 필터는 그 카테고리를 잃어 빈 목록·바 표시 | ⬜ | 필터 자동 해제는 미구현 — 확인 후 결정 |
| CAT-12 | 관리 화면 ⋮ → 위로/아래로 | 순서가 필터 시트·피커·칩에 반영, 재시작 후 유지 | ⬜ | 드래그 정렬은 3단계 후보 |
| CAT-13 | 상세보기 ⋮ → 카테고리 편집 | 한 항목만 대상, 정보 패널 칩 갱신 | ⬜ | ⋮ → 카테고리 편집, 정보 패널 "카테고리" 행 |
| CAT-14 | 다른 앱(삼성 갤러리)으로 사진 영구 삭제 후 앱 복귀 | 고아 할당 정리(관리 화면 개수 감소), 크래시 없음 | ⬜ | 전체 접근 권한일 때만, 2초 디바운스 |
| CAT-15 | 설정에서 "썸네일에 카테고리 색 점 표시" 끄기 | 썸네일 점 사라짐, 필터·칩은 그대로 | ⬜ | `UserPreferences.showCategoryBadges` |
| CAT-16 | 앱에서 영구 삭제(선택 → 완전 삭제) | 할당 즉시 제거(관리 화면 개수 감소) | ⬜ | `onActionDone(Delete)` |
