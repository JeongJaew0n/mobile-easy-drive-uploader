# 02 · Google 계정 / Drive / 업로드

## 사전 준비 (한 번)

GCP 콘솔에서: Drive API 사용 설정 → OAuth 동의 화면(외부·테스트, 테스트 사용자에 본인 계정, 범위 `.../auth/drive`) →
OAuth 클라이언트 ID(Android): 패키지 **`com.jjw.easygallery.debug`**, SHA-1 `A1:10:A5:5F:91:AC:1F:A8:65:0A:5F:26:B8:A7:31:21:5F:E0:14:38`.

| ID | 시나리오 | 기대 결과 | 상태 | 메모 |
|---|---|---|---|---|
| DRV-01 | GCP OAuth 클라이언트 등록 | 위 사전 준비 완료 | ❌ 2026-09-09 gms 로그 `UNREGISTERED_ON_API_CONSOLE` — `com.jjw.easygallery.debug` + 위 SHA-1 클라이언트 미등록/불일치. 등록 후 재시도 | 미등록 시 `DEVELOPER_ERROR`(status 10). 2026-09-09 로그인 실패 보고 → 원인 후보·확정 절차는 `../GOOGLE_SIGN_IN_TROUBLESHOOTING.md` |
| DRV-02 | 설정 → Google 계정 연결 | 계정 선택 → Drive 전체 접근 동의 → 스낵바 "연결되었습니다", 카드에 이메일·이름·저장공간 | ⬜ | scope 는 `drive`(전체) |
| DRV-03 | 동의 화면에서 취소 | "로그인이 취소되었습니다", 상태 변화 없음 | ⬜ | |
| DRV-04 | 연결 해제 | 카드가 연결 전 상태로, 업로드 큐도 취소됨 | ⬜ | |
| DRV-05 | 앱 재시작 후 | 로그인 유지(DataStore), 저장공간 다시 조회 | ⬜ | |
| DRV-06 | Drive 파일 보기 → 내 드라이브 | 폴더 먼저·이름순, 파일에 크기·수정일 | ⬜ | |
| DRV-07 | 100개 넘는 폴더에서 끝까지 스크롤 | 다음 페이지 자동 로드, 하단 로딩 표시 | ⬜ | pageSize 100 |
| DRV-08 | 하위 폴더 진입 → 뒤로 | 상위 폴더로 복귀(스택) | ⬜ | |
| DRV-09 | 새 폴더 만들기 | 목록에 정렬 위치대로 즉시 삽입 + 스낵바 | ⬜ | |
| DRV-10 | 파일 탭 | Drive 앱(있으면) 또는 브라우저로 열림 | ⬜ | `webViewLink` |
| DRV-11 | "이 폴더를 업로드 폴더로 지정" | 설정 화면으로 복귀(Drive 스택 전부 pop), 업로드 폴더 이름 갱신 | ⬜ | |
| DRV-12 | 업로드 폴더 미지정 상태에서 첫 업로드 | Drive 에 "Easy Gallery" 폴더가 자동 생성되고 그 안에 업로드 | ⬜ | appProperties `easyGalleryRoot=true` |
| DRV-13 | Drive 파일 행 ⋮ → 이름 변경 | 목록이 즉시 새 이름·정렬 위치로, 스낵바 "이름을 …(으)로 바꿨습니다". 실패 시 원래대로 + 오류 | ⬜ | `PATCH files/{id}` — `../DRIVE_FILE_CRUD.md` |
| DRV-14 | ⋮ → 이동 → 폴더 선택기에서 하위 폴더 진입·경로 탭으로 복귀 → "여기로 이동" | 항목이 목록에서 빠지고 대상 폴더에 존재. 원래 폴더에서는 버튼 비활성, 폴더 이동 시 자기 자신 제외 | ⬜ | `addParents/removeParents` |
| DRV-15 | ⋮ → 휴지통으로 이동 → 스낵바 "실행 취소" | 즉시 빠짐 → 실행 취소로 원래 위치에 복귀. Drive 웹 휴지통에도 반영 | ⬜ | `trashed` 토글 |
| DRV-16 | 폴더에 ⋮ 메뉴 | 열기 없음(이름 변경·이동·휴지통만) | ⬜ | |
| DRV-17 | 파일 행 길게 누름 → 다른 행 탭 → 상단 "2개 선택" → 삭제 | 두 항목 즉시 빠짐, 스낵바 "2개를 휴지통으로 옮겼습니다 · 실행 취소" → 실행 취소로 둘 다 복귀 | ⬜ | `DRIVE_FILE_CRUD.md` §7 |
| DRV-18 | 선택 모드 → 전체 선택 → 이동 → 폴더 선택 | 선택된 폴더 자신을 고르면 오류 스낵바, 다른 폴더면 전부 이동 + "n개를 …(으)로 옮겼습니다". 하단 진행바 "n / total 개 처리 중" | ⬜ | 순차 처리 |
| DRV-19 | 선택 모드에서 뒤로 가기 | 화면을 나가지 않고 선택만 해제 | ⬜ | `BackHandler` |
| DRV-21 | Drive 브라우저 상단 🔍 → "IMG" 입력 | 0.35초 뒤 전체 Drive 에서 이름이 IMG 로 시작하는 파일·폴더 목록(휴지통 제외). 행 ⋮ 에 "이동" 없음, 하단 버튼 없음. 뒤로 가기 → 원래 폴더 | ⬜ | `DRIVE_FILE_CRUD.md` §8 |
| DRV-22 | S3/WebDAV/SMB 브라우저 🔍 → 입력 | 원격 호출 없이 현재 폴더 목록이 즉시 걸러짐(힌트 "이 폴더에서 이름으로 찾기"), 이동 메뉴 유지, 뒤로 가기로 원래 목록 즉시 복귀 | ⬜ | 로컬 필터 |
| DRV-26 | 이 기기에서 업로드한 파일이 있는 Drive 폴더 | 그 파일 행 세부에 "· 이 기기에서 올림" | ⬜ | `uploaded_media` 원장 |
| DRV-23 | 파일 행 ⋮ → "기기에 저장" | 스낵바 "1개 다운로드를 시작했습니다", 알림 "기기에 저장 중 · 이름" 진행 → "저장 완료". 갤러리 Pictures/Easy Gallery(영상은 Movies)에 원본 화질로 나타남 | ⬜ | `DRIVE_FILE_CRUD.md` §9 |
| DRV-24 | 전체 선택(폴더 포함) → ⬇ | 폴더는 건너뛰고 파일 수만큼 "n개 다운로드", 각 파일이 순차/병렬로 저장. 같은 파일을 다시 누르면 진행 중 워크 유지 | ⬜ | `ExistingWorkPolicy.KEEP` |
| DRV-25 | S3/WebDAV/SMB 브라우저에서 "기기에 저장" | 동일 동작(SMB 는 Wi-Fi 필요) | ⬜ | 네 제공자 모두 DOWNLOAD |
| DRV-27 | 브라우저 목록을 아래로 당김 | 상단에 당겨서 새로고침 인디케이터, 목록은 유지된 채 갱신. 빈 폴더/오류 화면에서는 당기기 없음(새로고침 버튼 사용) | ⬜ | `PullToRefreshBox` |
| DRV-20 | S3/WebDAV/SMB 브라우저에서 다중 선택 → 삭제 | "n개 항목 삭제" 확인 다이얼로그(휴지통 없음) 후 영구 삭제, 실행 취소 없음 | ⬜ | |
| UPL-01 | 사진 3장 선택 → 업로드 버튼 (Android 13+) | 알림 권한 요청 → 거부해도 큐 등록 → 스낵바 "3개 항목을 대기열에 추가" | ⬜ | |
| UPL-02 | 업로드 진행 | 갤러리 상단 진행 배너(n/total·파일명), 상태바에 진행 알림(dataSync FGS) | ⬜ | |
| UPL-03 | 완료 | 요약 알림 "n개 항목을 업로드했습니다", Drive 폴더에 파일 존재, appProperties `mediaStoreId` | ⬜ | |
| UPL-04 | 대용량 영상(≥500MB) 업로드 중 **앱 강제 종료** | 재시작 시 큐가 재예약되고 세션 상태 조회 후 **끊긴 지점부터** 이어 올림 | ⬜ | `queryStatus` 308 경로 |
| UPL-05 | 업로드 중 비행기 모드 | 항목 PENDING 유지, attemptCount+1, 네트워크 복구 시 자동 재개 | ⬜ | 백오프 30s~ |
| UPL-06 | Wi-Fi 전용 ON + 모바일 데이터만 | 대기, Wi-Fi 연결 시 시작 | ⬜ | |
| UPL-07 | 업로드 중 설정에서 Wi-Fi 전용 토글 | REPLACE 재예약, 진행 중 항목이 처음부터가 아니라 이어서 | ⬜ | |
| UPL-08 | 충전 중에만 ON | 충전기 뽑으면 대기 | ⬜ | |
| UPL-09 | 이미 대기 중인 사진을 다시 업로드 | "n개 추가, m개는 이미 대기 중" | ⬜ | |
| UPL-10 | 업로드 목록: 실패 재시도 / 완료 정리 / 전체 취소 / 개별 삭제 | 각각 상태 반영, 전체 취소 시 알림도 사라짐 | ⬜ | |
| UPL-11 | 로그인 만료 상태에서 업로드 | 알림 "업로드가 중단되었습니다 — 다시 연결", 항목은 PENDING 유지 | ⬜ | 토큰 강제 만료 재현 어려움 → 계정 연결 해제로 대체 |
| UPL-12 | 원본을 지운 뒤 업로드 순서 도달 | 해당 항목만 FAILED(파일 없음), 다음 항목 계속 | ⬜ | |
| UPL-13 | 상세보기에서 업로드 버튼 | 단건 큐 등록 + 스낵바 | ⬜ | |
| DRV-P1 | `drive.file` + `PICKER_OAUTH_TRIGGER` 로 피커 띄우기 | 피커가 뜬다 | ✅ 2026-09-24 | GMS 가 `docs.google.com/picker/v2` 를 내부 WebView 로 띄운다. 새 라이브러리·웹 client ID 불필요. 매번 "확인하지 않은 앱" → "선택하려는 파일의 경우…" 동의를 거친다(`setPrompt(CONSENT)` 때문) |
| DRV-P2 | 피커에서 **파일** 선택 | 고른 파일 ID 가 돌아온다 | ✅ 2026-09-24 | 탭하면 선택되고 하단에 "N개 선택됨 / 삽입" 바. 결과는 `tokenResponseParams` 의 **`picked_file_ids`** (문자열). 복수 선택 시 구분자는 아직 미확인 |
| DRV-P3 | 피커에서 **폴더** 선택 | 폴더를 고를 수 있다 | ❌ 2026-09-24 | `PICKER_ALLOW_FOLDER_SELECTION="true"` 를 줘도 **폴더는 탭하면 안으로 들어갈 뿐 선택되지 않는다.** 목록/바둑판 보기 모두 체크박스가 없고, "파일 형식" 필터에도 폴더 항목이 없다. 피커 URL 에 플래그는 실려 갔다(`[1,1,null,1]`) — 값 형식 문제가 아니라 피커 UI 가 지원하지 않는 것으로 보인다. **이게 되지 않으면 "사용자가 지정한 폴더로 업로드" 가 성립하지 않는다** |
| DRV-P4 | `PICKER_MIMETYPES` 에 폴더 MIME 만 지정 | 폴더만 보인다 | ❌ 2026-09-24 | 폴더 목록은 나오지만 폴더 안이 항상 비어 보이고 선택 바도 뜨지 않는다. 이 값은 "고를 수 있는 형식"이라 폴더만 주면 아무것도 고를 수 없는 상태가 된다 |
| DRV-P5 | 피커로 고른 **파일의 부모 폴더**까지 권한이 미치는가 | 부모에 업로드할 수 있는지 | ✅ 2026-09-24 | **된다.** 고른 `Build.zip` → `parents=[1Wna7…]` 는 읽힌다. 그 폴더를 `files.get` 하면 **404**(폴더 자체는 못 읽는다), `files.list` 하면 접근권 있는 항목만 나온다(고른 파일 하나), 그런데 그 폴더를 `parents` 로 준 **`files.create` 는 성공**한다. Drive 앱에서 `Games` 안에 `EG-probe` 폴더가 실제로 생긴 것을 눈으로 확인하고 지웠다 |
| DRV-P6 | 부모 폴더의 **이름**을 얻을 수 있는가 | 화면에 무슨 폴더인지 보여줄 수 있는지 | ❌ 2026-09-24 | `files.get(parentId)` 가 404 라 이름을 알 길이 없다. 업로드는 되는데 "어디에 올리는지" 를 앱이 말해줄 수 없다 — 사용자가 직접 이름을 붙이게 하는 수밖에 |
| DRV-P7 | 설정에서 "Drive 폴더 지정" → 파일 선택 → 별칭 입력 | 지정 폴더 목록에 뜬다 | ✅ 2026-09-25 | 피커 → Games 안의 Build.zip 선택 → "이 폴더를 뭐라고 부를까요?" → GamesFolder 로 등록 |
| DRV-P8 | Drive 화면 루트 | 지정 폴더 + Easy Gallery 만 보인다 | ✅ 2026-09-25 | 루트에 GamesFolder 와 Easy Gallery 만. 사용자의 다른 폴더(상해·거제·Notability 등)는 하나도 안 보인다 |
| DRV-P9 | 루트에서 새 폴더 버튼 | 보이지 않는다 | ✅ 2026-09-25 | 루트 상단은 뒤로·검색·새로 고침 셋뿐. 다른 폴더 안에서는 새 폴더가 그대로 있다 |
| DRV-P10 | 지정 폴더 행의 ⋮ | 이름 변경·이동·삭제가 없다 | ✅ 2026-09-25 | 메뉴가 통째로 없다. 처음엔 빈 팝업이 떠서 `EntryMenu.hasAny` 로 ⋮ 자체를 내렸다 — `menu.delete` 가 화면에서 쓰이지 않던 것도 이때 드러났다 |
| DRV-P11 | 지정 폴더로 업로드 | 올라가고, Drive 앱에서 그 폴더에 보인다 | ✅ 2026-09-25 | GamesFolder 를 업로드 폴더로 지정 → 사진 1장 업로드 → **Drive 앱의 Games 안에 원래 파일들과 나란히** 들어갔다. 우리 앱에서는 그 사진과 피커로 고른 Build.zip 만 보이고 kokoa_day10.css/html 은 안 보인다 — 설계대로 |
| DRV-P12 | `setOptOutIncludingGrantedScopes(true)` 적용 후 로그인 | `grantedScopes=[drive.file]` 만 | ✅ 2026-09-25 | `grantedScopes=[drive.file]` 만. 이전 `drive` 가 딸려 오지 않는다 |
| UPL-14 | Wi-Fi 전용인데 LTE 일 때 배너 | "Wi-Fi 를 기다리는 중 (0/N)" | ⬜ | 2026-09-25 수정 전에는 "업로드 중 (1/N)" 이라 멈춘 줄 모른다 |
| UPL-15 | 충전 전용인데 충전 중이 아닐 때 | "충전을 기다리는 중 (0/N)" | ⬜ | |
| UPL-16 | 전송 실패로 백오프 대기 중 | "다시 시도하는 중 (n/N)" | ⬜ | 502 처럼 일시 오류로 쉬는 동안 |
| UPL-17 | 제약이 모두 맞을 때 | "업로드 중 (n/N) · 파일명" | ⬜ | 기존 문구 그대로여야 한다 |
| UPL-18 | 업로드 실패 메시지 | 서버가 준 설명이 함께 보인다 | ⬜ | 코드만 있으면 우리 잘못인지 서버 문제인지 구분이 안 된다 |
| UPL-19 | 100장 이상 한 번에 업로드 | 끊김 없이 모두 올라간다 | ✅ 2026-09-26 | 663장 전부 성공, 중복 0 |
| UPL-20 | 대량 업로드 속도 | 장당 1초 이내 | ✅ 2026-09-26 | 0.62s/장 (전 3.32s). 663장 6.6분 |
| UPL-21 | 4MB 초과 파일 | resumable 경로로 간다 | ✅ 2026-09-26 | 영상 7건, PUT 17~28초. 작은 파일은 multipart |
| UPL-22 | 업로드 중 취소 | 네 코루틴이 모두 멈춘다 | ✅ 2026-09-26 | 취소 후 백업 수가 더 늘지 않음 |
| UPL-23 | 업로드 중 앱 강제 종료 | 재시작하면 이어서 올린다 | ✅ 2026-09-26 | 30/663 에서 죽였고 61/663 으로 이어짐 |
| UPL-24 | 한도 초과(403) | 재시도하고 결국 성공한다 | ✅ 2026-09-26 | 2차에서 1건 발생 → 분류 수정 후 3·4차 0건 |
| UPL-25 | 업로드 중 화면 배너 | 완료 개수가 어긋나지 않는다 | ✅ 2026-09-26 | 병렬이어도 완료 기준이라 단조 증가 |
| UPL-26 | 1000장 규모 | 10분 내외 | ✅ 2026-09-26 | 1051장 733초(12.2분), 장당 0.70초. 규모가 커져도 거의 선형 |
| UPL-27 | 토큰이 만료된 채 대량 업로드 | 새 토큰을 받아 이어간다 | ⬜ | 1051장 실행 중 401 로 1건이 영구 실패했다 → GMS 토큰 캐시까지 비우도록 수정. 재현이 어려워 단위 테스트로만 덮음 |
| UPL-28 | 1550장(기존 1050 + 신규 500) 혼합 | 이미 올린 것은 건너뛴다 | ✅ 2026-09-26 | 중복 0, 영구 실패 0, 일시 실패 0 |
| UPL-29 | 업로드 중 강제 종료 → 재시작 | 이미 올라간 것은 다시 올리지 않는다 | ✅ 2026-09-26 | 80장 중 21장째에 종료 → `already on server, skipping` 4건(병렬 수와 일치), Drive 에 중복 없음 |
| UPL-30 | 이미 올린 사진을 다시 선택해 업로드 | 큐에 들어가지 않고 이유를 알려준다 | ✅ 2026-09-26 | "42개 모두 이미 올렸습니다". 일부만 겹치면 "N개 추가, M개는 이미 올렸거나 대기 중" |
| DRV-28 | Drive 화면에서 사진 누르기 | 계정 선택 없이 앱 안에서 바로 뜬다 | ✅ 2026-09-26 | `topResumedActivity` 가 우리 앱 그대로. 전체화면으로 이미지가 그려지고 X·탭으로 닫힌다. 기기에 Google 계정이 7개 있어도 묻지 않는다 |
| DRV-29 | 사진이 아닌 파일(zip·문서) 누르기 | 기존대로 외부 앱/브라우저로 연다 | ⬜ | `isImage` 가 아니면 `webViewLink` 폴백. 이 경로에서는 계정 선택이 여전히 뜰 수 있다 |
| DRV-30 | 권한 없는 파일을 미리보기 | "사진을 불러오지 못했습니다" | ⬜ | 403/404 시 에러 문구로 떨어지는지 |
| DRV-31 | Drive 루트에서 "볼 수 있는 폴더 추가" | 읽기 권한 동의 화면이 뜬다 | ⬜ | GCP 동의 화면에 `drive.readonly` 를 먼저 등록해야 한다(§10.6). 등록 전에는 authorize 가 실패한다 |
| DRV-32 | 동의 후 폴더 고르기 | 내 드라이브의 **실제 폴더들**이 이름 그대로 나열된다 | ⬜ | `drive.file` 일 때는 여기가 빈 목록이었다. 권한이 붙었는지 보는 가장 빠른 지표 |
| DRV-33 | 폴더를 고르고 "이 폴더 추가" | 루트 목록 맨 뒤에 "보기 전용" 배지와 함께 나온다 | ⬜ | 지정 폴더·Easy Gallery 뒤 |
| DRV-34 | 보기 전용 폴더에 들어가기 | **Drive 앱에서 보이던 파일이 전부 보인다** | ⬜ | 이 기능의 핵심. `drive.file` 로는 앱이 올린 것만 보였다 |
| DRV-35 | 보기 전용 폴더 안의 ⋮ | 열기·기기에 저장만 있고 이름 변경·이동·삭제가 없다 | ⬜ | |
| DRV-36 | 보기 전용 폴더 안의 하단 버튼 | "이 폴더를 업로드 폴더로 지정" 이 없다 | ⬜ | 읽기 권한뿐이라 올릴 수 없다 |
| DRV-37 | 보기 전용 폴더 안의 상단 | 새 폴더 버튼이 없다 | ⬜ | |
| DRV-38 | 보기 전용 폴더의 하위 폴더로 더 들어가기 | 거기서도 계속 보기 전용이다 | ⬜ | `DriveBrowserKey.readOnly` 가 전파되는지 |
| DRV-39 | 보기 전용 폴더의 사진 누르기 | 앱 안에서 뜬다(DRV-28 과 같다) | ⬜ | 읽기 권한이면 `alt=media` 도 받아진다 |
| DRV-40 | 보기 전용 폴더의 파일 "기기에 저장" | 갤러리에 들어온다 | ⬜ | 읽기 권한으로 다운로드가 되는지 |
| DRV-41 | 루트에서 보기 전용 폴더의 ⋮ → 목록에서 제거 | 목록에서만 빠지고 Drive 는 그대로 | ⬜ | Drive 앱에서 폴더가 남아 있는지 함께 확인 |
| DRV-42 | 동의 화면에서 읽기 권한 체크를 풀고 허용 | 폴더 고르기로 넘어가지 않고 안내가 뜬다 | ⬜ | `grantedScopes` 확인이 실제로 도는지 |
| DRV-43 | 보기 폴더를 추가한 뒤 업로드 | 종전대로 업로드 폴더에 올라간다 | ⬜ | scope 가 늘어도 업로드 경로가 깨지지 않는지 |
| DRV-44 | 보기 폴더 추가 후 앱 재시작 | 목록이 남아 있고 안이 보인다 | ⬜ | 토큰을 새로 받을 때 readonly 를 다시 싣는지(§10.2) |
| DRV-45 | 설정에서 계정 연결 해제 | 보기 폴더 목록이 사라진다 | ⬜ | 폴더 ID 는 계정에 종속된 값 |
